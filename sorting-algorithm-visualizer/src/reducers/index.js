const initialState = {
    sliderValue: '25',
    arrayToSort: []
}

const rootReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}
